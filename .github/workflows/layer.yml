name: Layer structure

on:
  pull_request:
  push:
    branches:
      - main
      - master

jobs:
  structural-check:
    runs-on: [self-hosted, Linux, X64, linux-builder]
    steps:
      - uses: actions/checkout@v4
      - name: Validate layer structure
        run: bash scripts/check-layer.sh
